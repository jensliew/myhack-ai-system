# Copy to .env.local in project root

# Next.js (port 3000)
GEMINI_API_KEY=your_gemini_key

# Express AI backend (port 3001) — see /ai-backend
USE_AI_BACKEND=true
AI_BACKEND_URL=http://localhost:3001

# Firebase — your existing Nexora config
# NEXT_PUBLIC_FIREBASE_API_KEY=
# ...
